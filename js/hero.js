$(document).ready(function() {
    function checWidth() {
        var windowWidth = $('body').innerWielem = $(".number-bullets"); //
                                                                       //
        if(windowWidth < 480){
            elem.removeClass('container');
            elem.addClass('container');

        }
        else{
            elem.removeClass('container');
            elem.addClass('container');
        }
    }
});
